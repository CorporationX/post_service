spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    username: user
    password: password



  profiles:
    active: dev

  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 50MB

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml

  data:
    redis:
      port: 6379
      host: localhost
      user-ban-topic: user_ban
      like-analytics-topic: like_analytics

      channels:
        calculations_channel:
          name: calculations_channel
        ad-bought-channel:
          name: ad_bought_channel
        post-channel:
          name: post_channel
      prefix-users: users
      prefix-posts: posts
      prefix-feeds: feeds
      max-feeds: 500
      ttl-users: 1
      ttl-posts: 1
      ttl-feeds: 1


  kafka:
    producer:
      bootstrap-servers: localhost:9092, localhost:9094
      group-id: feed
      retries: 10
      acks: all
      properties:
        delivery-timeout-ms: 10000
        linger-ms: 0
        request-timeout-ms: 5000
        idempotence: true
        max-in-flight-requests-per-connection: 5
    consumer:
      bootstrap-servers: localhost:9092, localhost:9094
      group-id: feed
      properties:
        spring.json.value.default.type: "faang.school.postservice.event"
        spring.json.trusted.packages: "faang.school.postservice, faang.school.postservice.event"
    topics:
      post-published: post-published-events-topic
      post-view: post-view-events-topic
      post-like: post-like-events-topic
      post-comment: post-comment-events-topic
      partitions: 3
      replicasCount: 3
      min-insync-replicas: 2

  task:
    execution:
      pool:
        core-size: 2
        max-size: 10
        queue-capacity: 500
      thread-name-prefix: async-worker-thread- # префикс для имен потоков
  scheduler:
    comment:
      moderator:
        path-curse-words: src/main/resources/curse-words.txt
        cron: 0 0 * * * *
        partition-size: 1000
        treads-count: 10
    post:
      publisher:
        cron: 0 0 * * * *
        partition-size: 1000
        threads-count: 10

server:
  port: 8975

logging:
  level:
    root: info

services:
  s3:
    endpoint: http://localhost:9000
    accessKey: user
    secretKey: password
    bucketName: resource-post

user-service:
  host: localhost
  port: 14546
project-service:
  host: localhost
  port: 8082
payment-service:
  host: localhost
  port: 9080

