spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    username: user
    password: password

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml

  data:
    redis:
      port: 6379
      host: localhost
      max-total: 128
      max-idle: 128
      min-idle: 16
      channels:
        calculations_channel:
          name: calculations_channel
        post_like_channel:
          name: post_like_events

  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: my-group
      auto-offset-reset: earliest
    topic:
      like:
        name: likes
        partitions: 3
        replication-factor: 1

server:
  port: 8081

logging:
  level:
    root: info

user-service:
  host: localhost
  port: 8080
project-service:
  host: localhost
  port: 8082
payment-service:
  host: localhost
  port: 9080

ad-cleanup:
  executor:
    core-size: 3
    max-size: 10
    queue-capacity: 50
    thread-name-prefix: "ad-cleanup-"
  scheduled:
    cron: "0 0 3 * * ?"
    batch-size: 100

services:
  s3:
    endpoint: http://192.168.0.142:9000
    accessKey: user
    secretKey: password
    bucketName:
      name: corpbucket

moderation:
  cron:
    expression: 0 0 0 * * ?
  dictionary:
    path: "dictionary-for-moderation.txt"
    batch:
      size: 1000

kafka:
  bootstrap-servers: localhost:9092
  topic:
    post:
      name: posts
      partitions: 1
      replicationFactor: 1
      async:
        core-pool-size: 5
        max-pool-size: 10
        queue-capacity: 20

feed:
  max-size: 500