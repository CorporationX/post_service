spring:
  multipart:
    max-file-size: 5
    max-request-size: 5
  mvc:
    servlet:
      path: /api/v1
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    username: user
    password: password

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml

  data:
    redis:
      port: 6379
      host: localhost
      channels:
        calculations_channel:
          name: calculations_channel
  task:
    scheduling:
      comment:
        max_comments_per_size: 20
        cron_expression: 0 0 0 * * *
        executor:
          core_pool_size: 10
          max_poll_size: 20
          queue_capacity: 200
          thread_prefix: comment-moderator

server:
  port: 8081

logging:
  level:
    root: info

user-service:
  host: localhost
  port: 8080
project-service:
  host: localhost
  port: 8082
payment-service:
  host: localhost
  port: 9080

dictionary:
  comments:
    path_to_file: /dictionary/comments/badWords.txt
post-service:
  tag:
    count-of-most-popular-tags: 2

s3:
  endpoint: http://localhost:9000
  accessKey: user
  secretKey: password
  bucketName: corpbucket

image:
  resize-sizes:
    - 1080
    - 170