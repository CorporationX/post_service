spring:
  config:
    import: optional:secrets.yaml
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    username: user
    password: password

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml

  data:
    redis:
      port: 6379
      host: localhost
      channels:
        calculations_channel:
          name: calculations_channel

    kafka:
      bootstrap-servers: localhost:29092
      topics:
        post-channel: posts
        feed-heat: feed-heat-topic

server:
  port: 8081

logging:
  level:
    root: info


post:
  publisher:
    scheduler:
      cron: "0 0/1 * 1/1 * ?" # every minute
  ad-remover:
    scheduler:
      cron: "0 0/1 * 1/1 * ?" # every minute
    threads-count: 20
    max-list-size: 100

user-service:
  host: localhost
  port: 8080
project-service:
  host: localhost
  port: 8082
payment-service:
  host: localhost
  port: 9080

thread-pool:
  size: 10
cron:
  spelling-check: 0 0 3 * * *

side-api:
  text-gears:
    key: ${secrets.text-gears-api-key}
    base-url: https://api.textgears.com
    auto-correct: /correct